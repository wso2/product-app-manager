<%
//auto rediecting the user to SSO
var user = request.getParameter('user'),
        loged = require('/modules/user.js').current(session);
if (user && !loged) {
    include('login.jag');
} else {
    print('auto logged.');
}
%>
